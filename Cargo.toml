[package]
name = "ttop"
version = "0.1.0"
edition = "2024"
description = "A tiny TUI that lists, filters and kills the processes running in every tmux pane."
license = "MIT"

[dependencies]
ratatui = "0.30.2"
sysinfo = { version = "0.39.5", default-features = false, features = ["system"] }
